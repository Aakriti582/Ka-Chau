services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: kachau
      POSTGRES_USER: kachau
      POSTGRES_PASSWORD: samarpan12
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

